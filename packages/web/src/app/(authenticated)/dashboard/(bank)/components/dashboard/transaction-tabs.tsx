'use client';

import { UnifiedActivity } from './unified-activity';

export function TransactionTabs() {
  // Now we use a single unified activity component that handles both modes
  return <UnifiedActivity />;
}
