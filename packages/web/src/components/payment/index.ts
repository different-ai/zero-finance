// NOTE: This directory used to export a pay button, but the implementation
// now lives under `src/components/invoice/`.
// Keeping this file as an empty module avoids breaking builds due to a stale export.
export {};
